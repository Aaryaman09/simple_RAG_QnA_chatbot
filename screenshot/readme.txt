In image RAG_with_session_id

I had switched session_id from chat_session_1 -> chat_session_2 and captured the result 

It was visible, after switching, it had not context about "self reflection"

So i switched it back to chat_session_1 and suddenly it remmebers what is "it"